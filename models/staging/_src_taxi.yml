sources:
  - name: src_nyc_taxi
    database: nyc_taxi
    schema: raw
    tables:
      - name: yellow_taxi_trips
        description: "Raw yellow taxi trips stored as VARIANT parquet data"
        columns:
          - name: raw_data
            tests:
              - not_null
            
      - name: green_taxi_trips
        description: "Raw green taxi trips stored as VARIANT parquet data"
        columns:
          - name: raw_data
            tests:
              - not_null